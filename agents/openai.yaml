interface:
  display_name: "ClearFlow"
  short_description: "Guided Brief, BDD, tests, handoff, review"
  default_prompt: "Use $clearflow to guide this feature from Brief through PRD/BDD, test strategy, architecture, handoff, review, and progress."

policy:
  allow_implicit_invocation: false
