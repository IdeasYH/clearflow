interface:
  display_name: "ClearFlow"
  short_description: "Guided Brief, BDD, tests, review agents, handoff"
  default_prompt: "Use $clearflow to guide this feature from Brief through PRD/BDD, test strategy, review agents, architecture, handoff, acceptance review, and progress."

policy:
  allow_implicit_invocation: false
